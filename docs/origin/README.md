# docs/origin

Prima-clock: 202609221628

Source documents that shaped this repo. They are held here unchanged, not
active guidance. For how naught actually works now, read the root `CLAUDE.md`,
`schema/naught_intake.json` and `docs/open-questions.md`.

| Document | What it became |
|---|---|
| `prima-template-CLAUDE.md` | The prima template's root `CLAUDE.md`. Moved here when naught received its own (#2). |
| `handoff_to_code_session_naught_maw_nullus` (`.docx` + `.md`) | The Act II handoff from the Custos chat space. Items 1–4 are done: #2 added `CLAUDE.md`, the no-erasure review ran on #1, #1 built the scaffold, and eaprime1/nullus#3 drafted the nullus `CLAUDE.md`. Item 5 (maw) is held. The document's own `Prima-clock:` line was left to fill in at commit time. The stamp for this session is **202609221628**. |
| `naught-CLAUDE` (`.docx` + `.md`) | Became the root `CLAUDE.md` in #2, with three review corrections: the secret name, a valid data-model example, and the Maw ordering marked provisional. |
| `naught-repo-copilot-instructions` (`.docx` + `.md`) | An earlier draft of the same content for Copilot. The handoff says `CLAUDE.md` supersedes it. It was also the prompt Copilot built #1 from. Kept here for the record. |

Each `.md` is a witness copy of its `.docx`, so the originals can be read in a
review. The wording is the same.
