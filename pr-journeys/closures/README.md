# Closures

Stale-branch closure notes — one file per closed branch, named
`BRANCHNAME.md`. See `docs/stale-branch-closure.md` for the full
procedure. This README exists only so git tracks the directory before
the first real closure lands; delete it once real closure files are here
if you'd rather the directory speak for itself.
